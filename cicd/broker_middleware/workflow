
# docker login

# go the service directory
docker build -t ${DOCKER_REGISTRY}/broker-middleware:v1 .

# push the docker image to docker hub
docker push ${DOCKER_REGISTRY}/broker-middleware

# helm install
helm upgrade --install broker-middleware . --values values.yaml